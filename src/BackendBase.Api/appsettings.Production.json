{
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Database": {
    "//Provider": "Switch to SqlServer or PostgreSql and supply ConnectionStrings:DefaultConnection via a secrets manager.",
    "Provider": "SqlServer"
  },
  "Jwt": {
    "Issuer": "BackendBase",
    "Audience": "BackendBaseClients",
    "//SigningKey": "MUST be supplied at runtime via a secrets manager / Key Vault / env var. Left empty here on purpose; the app fails fast at startup if it is missing.",
    "SigningKey": "",
    "ExpiryMinutes": 60
  }
}
