{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Database": {
    "Provider": "InMemory"
  },
  "Jwt": {
    "Issuer": "BackendBase",
    "Audience": "BackendBaseClients",
    "//SigningKey": "DEV ONLY throwaway key so the API runs out of the box. Never reuse in any real environment; supply real keys via user-secrets or a secrets manager.",
    "SigningKey": "dev-only-super-secret-signing-key-change-me-32+chars",
    "ExpiryMinutes": 120
  }
}
